# NodeCRM - Система управления взаимоотношениями с клиентами

NodeCRM - это современная CRM система, построенная на Node.js с минимальным количеством зависимостей и файловым роутингом.

## Возможности

- 📊 **Управление клиентами** - добавление, редактирование и отслеживание клиентов
- 🎯 **Управление лидами** - работа с потенциальными клиентами
- ✅ **Система задач** - создание и отслеживание задач
- 📈 **Дашборд с аналитикой** - обзор ключевых метрик
- 📋 **Отчеты** - детальная аналитика и экспорт данных
- 🔄 **Отслеживание активности** - журнал всех действий в системе

## Архитектура

### Файловый роутинг

Система использует файловый роутинг - каждый файл в папке `routes/` автоматически становится маршрутом:

- `routes/logger.js` → `/`
- `routes/clients.js` → `/clients`
- `routes/leads.js` → `/leads`
- `routes/tasks.js` → `/tasks`
- `routes/api/dashboard.js` → `/api/dashboard`

### Минимальные зависимости

Проект использует только необходимые зависимости:

- **pg** - для работы с PostgreSQL
- **chalk** - для красивого вывода в консоли
- Никаких фреймворков! Чистый Node.js

### Структура проекта

```
NodeCRM/
├── app.js                 # Основной сервер
├── routes/                # Файловые маршруты
│   ├── logger.js          # Главная страница
│   ├── clients.js        # Управление клиентами
│   ├── leads.js          # Управление лидами
│   ├── tasks.js          # Управление задачами
│   ├── reports.js        # Отчеты
│   └── api/
│       └── dashboard.js  # API для дашборда
├── lib/
│   └── database/
│       └── logger.js      # Работа с базой данных
├── public/               # Статические файлы
│   ├── css/
│   │   └── style.css     # Стили интерфейса
│   └── js/               # Клиентские скрипты
│       ├── dashboard.js
│       ├── clients.js
│       ├── leads.js
│       ├── tasks.js
│       └── reports.js
└── package.json
```

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных PostgreSQL

Убедитесь, что PostgreSQL установлен и запущен. Создайте базу данных:

```sql
CREATE DATABASE nodecrm;
```

### 3. Настройка переменных окружения

Создайте файл `.env` (или используйте переменные окружения):

```bash
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nodecrm
DB_USER=postgres
DB_PASSWORD=your_password
PORT=3000
NODE_ENV=development
```

### 4. Запуск приложения

```bash
npm start
```

Или для разработки:

```bash
node app.js
```

## Использование

### Веб-интерфейс

Откройте браузер и перейдите по адресу: `http://localhost:3000`

### Основные функции

#### Дашборд

- Обзор ключевых метрик
- Последние действия в системе
- Быстрый доступ к основным разделам

#### Управление клиентами

- Добавление новых клиентов
- Редактирование информации о клиентах
- Поиск и фильтрация
- Отслеживание статуса клиентов

#### Управление лидами

- Создание и отслеживание лидов
- Управление воронкой продаж
- Конвертация лидов в клиентов
- Отслеживание источников лидов

#### Система задач

- Создание и назначение задач
- Отслеживание статуса и приоритета
- Связывание задач с клиентами и лидами
- Управление сроками выполнения

#### Отчеты и аналитика

- Статистика по клиентам, лидам и задачам
- Экспорт данных
- Анализ производительности

## API

### Эндпоинты

#### Клиенты

- `GET /clients` - список клиентов
- `POST /clients` - создание клиента
- `PUT /clients` - обновление клиента
- `DELETE /clients?id=X` - удаление клиента

#### Лиды

- `GET /leads` - список лидов
- `POST /leads` - создание лида

#### Задачи

- `GET /tasks` - список задач
- `POST /tasks` - создание задачи

#### Дашборд

- `GET /api/dashboard` - статистика для дашборда

## База данных

Система автоматически создает необходимые таблицы при первом запуске:

### Таблица clients

- id, name, email, phone, company, status, notes, created_at, updated_at

### Таблица leads

- id, name, email, phone, company, source, status, value, probability, notes, assigned_to, created_at, updated_at

### Таблица tasks

- id, title, description, status, priority, assigned_to, client_id, lead_id, due_date, created_at, updated_at

### Таблица activities

- id, type, description, entity_type, entity_id, user_name, created_at

## Расширение функционала

### Добавление новых маршрутов

1. Создайте файл в папке `routes/`
2. Экспортируйте функции для HTTP методов:

```javascript
export function get(req, res) {
  /* ... */
}
export function post(req, res) {
  /* ... */
}
export function put(req, res) {
  /* ... */
}
export function del(req, res) {
  /* ... */
}
```

### Добавление middleware

```javascript
app.use((req, res) => {
  // Ваш middleware код
});
```

### Работа с базой данных

```javascript
import {Database} from './lib/schemas/logger.js';

const db = new Database();

// Использование методов БД
const clients = await db.getClients();
const client = await db.createClient(clientData);
```

## Безопасность

- Используйте переменные окружения для конфиденциальных данных
- Настройте правильные права доступа к базе данных
- В продакшене используйте HTTPS
- Регулярно обновляйте зависимости

## Развитие

Планируемые функции:

- Система авторизации и пользователей
- Более детальная аналитика и отчеты
- Интеграции с внешними сервисами
- API для мобильных приложений
- Система уведомлений
- Экспорт данных в различных форматах

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории проекта.
